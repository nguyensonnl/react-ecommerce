import React from "react";
import Layout from "../Layout";

const User = () => {
  return (
    <Layout>
      <div className="user">This is component user</div>
    </Layout>
  );
};

export default User;
